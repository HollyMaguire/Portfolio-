	function countingCharacters(){
        stringToCount = document.getElementById('in').value;
	    console.log (stringToCount + " has " + 
              stringToCount.length + " characters.");
              document.getElementById('output').innerText += (stringToCount + " has " + stringToCount.length + " characters.");
                }
        


